services:
  - type: web
    name: shri-sai-computer-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 5000
      - key: CLIENT_URL
        value: "*"
